from pathlib import Path
import os

# PCAP file output
PCAP_OUTPUT = Path("captures/notifications.pcap")

# Notification log file
NOTIFICATION_LOG = Path("notifications.log")

# Capture interface
CAPTURE_INTERFACE = "Ethernet 4"  # Update to your interface if needed
